# --- Secrets — NEVER commit ---
.env
*.local
**/settings.local.json

# --- Harness runtime state (created inside the target project, not here) ---
.bcf/

# --- Memory (data + local artifacts) ---
memory/pgvector/data/
memory/history/.venv/
memory/history/*.db
**/__pycache__/
*.pyc

# --- Node / build ---
node_modules/
dist/
test-results/
*.log
